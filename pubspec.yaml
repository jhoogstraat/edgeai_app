name: edgeai_app
description: The companion app for our paper.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^1.0.3
  multicast_dns:
    git: https://github.com/0xNF/flutter_multicast_dns
  socket_io_client: ^2.0.0-beta
  http: ^0.13.4
  modal_bottom_sheet: ^2.0.0
  horizontal_picker: ^1.2.0
  logging: ^1.0.2
  network_info_plus: ^2.1.2
  collection: ^1.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true