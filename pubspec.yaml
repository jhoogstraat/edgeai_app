name: obj_detect_board
description: The companion app for our paper.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^1.0.0-dev
  socket_io_client: ^2.0.0-beta
  http: ^0.13.3
  modal_bottom_sheet: ^2.0.0
  multicast_dns: ^0.3.1
  logging: ^1.0.2

dev_dependencies:
  flutter_lints: ^1.0.4
  
  flutter_test:
    sdk: flutter
  

flutter:
  uses-material-design: true