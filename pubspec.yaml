name: obj_detect_board
description: The companion app for our paper.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^0.12.1
  socket_io_client: ^0.9.12
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true